//A planned development tool that once hooked in will allow the user to set the current time. Allows developer to test events

export default class ClockTool {
  setTime() {
    let date = new Date();
  }
}